extraction:
  csharp:
    index:
      solution: "app/Spinner.sln"
      nuget_restore: true
      dotnet:
        version: "5.0.100"
      build_command:
      - "export PB_MODE=release"
      - "dotnet build ./app/Spinner.sln -f net5.0"